# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment variables (secrets)
.env.local
.env.production

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Docker compose env (secrets) — JANGAN commit
.env

# Cloudflare Tunnel credentials per-host
cloudflared-config/credentials.json

# Docker Compose project data (kalau nanti ada)
docker-data/

# 9Router runtime data (db, providers config — local only)
9router-data/*
!9router-data/.gitkeep

# SearXNG runtime config (per-deployment secret_key)
searxng-config/settings.yml

# Cloudflare Tunnel routing config (contains tunnel UUID per deployment)
cloudflared-config/config.yml

# Local backups of old git history (do NOT commit)
.git_backup_*/
